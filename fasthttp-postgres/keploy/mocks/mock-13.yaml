version: api.keploy.io/v1beta2
kind: SQL
name: mock-13-0
spec:
    metadata:
        arguments: '[{ 1 book} { 2 1958} { 3 2}]'
        name: SQL
        operation: ExecContext
        query: '"INSERT INTO books(title, year, author_id) VALUES ($1, $2, $3);"'
        type: SQL_DB
    type: error
    int: 0
    error:
        - nil
---
version: api.keploy.io/v1beta2
kind: SQL
name: mock-13-1
spec:
    metadata:
        arguments: '[{ 1 book} { 2 1958} { 3 2}]'
        name: SQL
        operation: ExecContext.LastInsertId
        query: '"INSERT INTO books(title, year, author_id) VALUES ($1, $2, $3);"'
        type: SQL_DB
    type: int
    int: 0
    error:
        - LastInsertId is not supported by this driver
---
version: api.keploy.io/v1beta2
kind: SQL
name: mock-13-2
spec:
    metadata:
        arguments: '[{ 1 book} { 2 1958} { 3 2}]'
        name: SQL
        operation: ExecContext.RowsAffected
        query: '"INSERT INTO books(title, year, author_id) VALUES ($1, $2, $3);"'
        type: SQL_DB
    type: int
    int: 1
    error:
        - nil
