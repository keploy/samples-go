version: api.keploy.io/v1beta2
kind: SQL
name: mock-7-0
spec:
    metadata:
        arguments: '[{ 1 1}]'
        name: SQL
        operation: QueryContext
        query: "\"SELECT b.id, b.title, b.year, b.author_id, a.first_name, a.last_name\n\t\t\t\t\t\t\tFROM authors a \n\t\t\t\t\t\t\tLEFT JOIN books b on a.id=b.author_id \n\t\t\t\t\t\t\tWHERE a.id = $1;\""
        type: SQL_DB
    type: error
    int: 0
    error:
        - nil
---
version: api.keploy.io/v1beta2
kind: SQL
name: mock-7-1
spec:
    metadata:
        name: SQL
        operation: QueryContext.Close
        type: SQL_DB
    type: table
    table:
        cols:
            - name: id
              type: int64
              precision: 0
              scale: 0
            - name: title
              type: string
              precision: 0
              scale: 0
            - name: year
              type: int64
              precision: 0
              scale: 0
            - name: author_id
              type: int64
              precision: 0
              scale: 0
            - name: first_name
              type: string
              precision: 0
              scale: 0
            - name: last_name
              type: string
              precision: 0
              scale: 0
        rows:
            - '[`1` | `Oliver Twist` | `1837` | `1` | `Charles` | `Dickens` | ]'
            - '[`2` | `David Copperfield` | `1849` | `1` | `Charles` | `Dickens` | ]'
            - '[`3` | `Great Expectations` | `1860` | `1` | `Charles` | `Dickens` | ]'
            - '[`15` | `hello` | `1898` | `1` | `Charles` | `Dickens` | ]'
            - '[`15` | `hello` | `1898` | `1` | `Charles` | `Dickens` | ]'
    int: 0
    error:
        - nil
        - nil
        - nil
        - nil
        - EOF
        - nil
